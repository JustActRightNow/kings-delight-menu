/* ── Centralised mutable app state ─────────────────────────────────────── */
const state = {
  plates: [{ id: 1, items: [], orderType: 'eat-in' }],
  activePlateIndex: 0,
  nextPlateId: 2,
  activeSection: 'eatery'
};
