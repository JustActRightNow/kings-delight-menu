/* ── config.template.js ───────────────────────────────────────────────────────
 * Copy this file to config.local.js and fill in your values.
 * NEVER commit config.local.js — it is in .gitignore.
 * In this project the SUPABASE_URL and SUPABASE_ANON constants are set
 * in js/config.js, which is loaded by both index.html and admin.html.
 * Replace the placeholder strings there with your real Supabase credentials.
 * ─────────────────────────────────────────────────────────────────────────── */
const SUPABASE_URL  = 'https://YOUR_PROJECT_ID.supabase.co';
const SUPABASE_ANON = 'YOUR_ANON_KEY';
